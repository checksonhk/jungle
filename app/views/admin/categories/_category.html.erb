<tr>
  <td colspan="2" >
    <h4><%= category.name %></h4>
  </td>
  <td>
    <h4><%= category.created_at %></h4>
  </td>
  <td>
    <h4><%= category.products.count %>
  </td>
  <td>
    <%= link_to fa_icon('trash'), [:admin, category], class: 'btn btn-sm btn-danger', method: :delete, data: { confirm: "Are you sure?" } %>
  </td>
</tr>